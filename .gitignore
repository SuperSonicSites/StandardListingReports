node_modules/
dist/
.astro/
.vercel/
.wrangler/
.tmp/

.env
.env.*
!.env.example

data/snapshots/*.json
!data/snapshots/.gitkeep

# Client profiles are runtime data on the production volume (they carry password
# hashes and uploaded-logo data URIs) — never committed, never touched by deploys.
data/clients/*.json
!data/clients/.gitkeep

*.log

.claude/
.codex-remote-attachments/
